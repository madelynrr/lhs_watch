<div class="border-2 border-black rounded p-2">
    <%= button_to "#{animal.favorite ? "FAVE" : "NOPE"}", animal_path(animal.id), method: :put, params: {animal: {favorite: !animal.favorite}} %>
</div>
