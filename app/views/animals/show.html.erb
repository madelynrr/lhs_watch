<p style="color: green"><%= notice %></p>
<div>
  <%= render @animal %>

  <div>
    <%= link_to "Edit this animal", edit_animal_path(@animal) %> |
    <%= link_to "Back to animals", animals_path %>

    <%= button_to "Destroy this animal", @animal, method: :delete, data: {turbo_confirm: "Are you sure?"} %>
  </div>
</div>
